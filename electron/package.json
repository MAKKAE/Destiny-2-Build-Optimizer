{
  "name": "d2-optimizer-desktop",
  "version": "1.0.0",
  "description": "Destiny 2 Build Optimizer Desktop Edition",
  "author": "Makka",
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "prebuild": "node scripts/prebuild.js",
    "prebuild:dir": "node scripts/prebuild.js",
    "build:backend": "node scripts/build-backend.js",
    "build:frontend": "npm run build --prefix ../frontend",
    "build": "npm run build:backend && npm run build:frontend && electron-builder --config electron-builder.yml",
    "build:dir": "npm run build:backend && npm run build:frontend && electron-builder --dir --config electron-builder.yml"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3"
  }
}
